{
  "columns": [
    {
      "rows": [
        {
          "type": "list",
          "options": {
            "listKey": "TransactionsOnBundle",
            "params": {
              "uniqueListKey": "{{recordId}}",
              "recordId": "{{parentId}}",
              "filters": {
                "invoiceBundle": {
                  "default": {
                    "value": "{{recordId}}"
                  }
                }
              }
            }
          }
        }
      ]
    }
  ]
}